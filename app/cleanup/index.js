import dynamic from "next/dynamic";

export const metadata = {
  title: "Jons Hung | Home",
  description: "Jons Hung Portfolio",
};

export default function HomePageComponent({ children }) {
  return ({children}
    
  );
}
